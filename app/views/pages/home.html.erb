<!-- ===== Wrapper page : fond clair + centrage global ===== -->
<div class="min-h-screen bg-gray-50 flex flex-col items-center py-16 px-4">
  <!-- Titre -->
  <h1 class="text-4xl font-bold text-gray-800 mb-4">Muso</h1>

  <!-- Sous-titre -->
  <p class="text-center text-lg text-gray-600 mb-12 max-w-2xl">
    Comédien, réalisateur et musicien, <em>Muso</em> mêle émotion brute et univers cinématographique dans son travail.
  </p>

  <!-- ===== Zone Vue : une seule section, pas de second min-h-screen ===== -->
  <div class="w-full max-w-4xl mx-auto px-2">
    <!-- Carte sombre sans contour dur (pas de border, ni ring) -->
    <div class="rounded-2xl bg-gray-900 text-gray-100 p-6 shadow-lg shadow-black/20">
      <!-- Mount Vue -->
      <div id="app" class="w-full"></div>
    </div>
  </div>
</div>
