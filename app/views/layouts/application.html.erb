<!DOCTYPE html>
<html>
  <head>
    <title>Muso</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>
    <!--
      If using a TypeScript entrypoint file:
        vite_typescript_tag 'application'

      If using a .jsx or .tsx entrypoint, add the extension:
        vite_javascript_tag 'application.jsx'

      Visit the guide for more information: https://vite-ruby.netlify.app/guide/rails
    -->

  </head>

  <body class="min-h-screen antialiased bg-gray-50 text-gray-900">
  <!-- Navbar gris translucide, sans noir plein -->
  <!-- NAVBAR collée en haut, même style que le player -->
<nav
  class="fixed inset-x-0 top-0 z-50
         bg-gray-900/70               <!-- même opacité que le player -->
         backdrop-blur                 <!-- même effet de flou -->
         border-b border-white/10">    <!-- léger séparateur, discret -->
  <div class="mx-auto max-w-6xl px-4 py-2 flex items-center justify-between">
    <!-- Logo / Titre -->
    <%= link_to "Muso", root_path,
      class: "font-semibold tracking-tight text-white" %>

    <!-- Actions à droite -->
    <div class="flex items-center gap-3">
      <%# Lien Admin visible uniquement pour un admin connecté %>
      <% if user_signed_in? && current_user.admin? %>
        <%= link_to "Admin", admin_dashboard_path,
          class: "px-3 py-1.5 rounded-lg bg-white/10 text-white hover:bg-white/20 transition" %>
      <% end %>

      <%# Connexion / Déconnexion %>
      <% if user_signed_in? %>
        <%= button_to "Se déconnecter", destroy_user_session_path, method: :delete,
          class: "px-3 py-1.5 rounded-lg border border-white/20 text-white hover:bg-white/10 transition" %>
      <% else %>
        <%= link_to "Se connecter", new_user_session_path,
          class: "px-3 py-1.5 rounded-lg border border-white/20 text-white hover:bg-white/10 transition" %>
      <% end %>
    </div>
  </div>
</nav>

<!-- Décalage pour ne pas cacher le contenu sous la navbar fixe -->
<div class="pt-12 md:pt-14"></div>
  <%= yield %>
</body>
</html>
