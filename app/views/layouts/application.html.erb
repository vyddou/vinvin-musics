<!DOCTYPE html>
<html>
  <head>
    <title>Muso</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>
    <!--
      If using a TypeScript entrypoint file:
        vite_typescript_tag 'application'

      If using a .jsx or .tsx entrypoint, add the extension:
        vite_javascript_tag 'application.jsx'

      Visit the guide for more information: https://vite-ruby.netlify.app/guide/rails
    -->

  </head>

  <body class="min-h-screen antialiased
             bg-gray-50 text-gray-900
             dark:bg-neutral-900 dark:text-gray-100">


  <!-- === Navbar globale === -->
  <nav class="flex items-center justify-between px-6 py-4 border-b border-gray-200 dark:border-gray-700">
    <div class="text-lg font-bold">
      <%= link_to "Muso", root_path, class: "hover:text-blue-600" %>
    </div>

    <div class="flex gap-4">
      <% if user_signed_in? && current_user.admin? %>
        <%= link_to "Admin", admin_dashboard_path, class: "px-3 py-2 rounded bg-slate-800 text-white" %>
      <% end %>

      <% if user_signed_in? %>
        <%= button_to "Se déconnecter", destroy_user_session_path, method: :delete,
              class: "px-3 py-2 rounded border" %>
      <% else %>
        <%= link_to "Se connecter", new_user_session_path, class: "px-3 py-2 rounded border" %>
      <% end %>
    </div>
  </nav>
  <!-- === Fin navbar === -->

    <%= yield %>
  </body>
</html>
